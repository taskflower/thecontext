// src/pages/settings/routes.tsx
import { RouteObject } from "react-router-dom";
import { Settings } from "./Settings";

const settingsRoutes: RouteObject[] = [{ index: true, element: <Settings /> }];

export default settingsRoutes;
