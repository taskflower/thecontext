import { ConfigProvider, useConfig } from './ConfigProvider';
import { preloadModules, getConfigIdFromURL, loadJsonConfigs } from './preload';

export {
  ConfigProvider,
  useConfig,
  preloadModules,
  getConfigIdFromURL,
  loadJsonConfigs
};

export default ConfigProvider;