// src/templates/default/workspaces/index.ts
import { Workspace } from '../../baseTemplate';
import { marketingWorkspace } from './marketing';

export const workspaces: Workspace[] = [
  marketingWorkspace,
  // Inne workspace'y...
];